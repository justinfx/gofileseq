name: ci
on: [push, pull_request]

jobs:
  test_go:
    name: Test go-${{ matrix.go }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [
          'ubuntu-latest',
          'macOs-latest',
          'windows-latest'
        ]
        go: ['1.12.9']
    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Setup go
        uses: actions/setup-go@v1
        with:
            go-version: ${{ matrix.go }}

      - name: Test
        run: go test -v ./...

  test_cpp:
    name: Test cpp
    runs-on: "ubuntu-latest"
    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Setup waf
        env:
          WAF_VERSION: waf-1.9.14
        run: test -x waf-light || {
          cd $HOME &&
          wget "https://gitlab.com/ita1024/waf/-/archive/${WAF_VERSION}/waf-${WAF_VERSION}.tar.gz" &&
          tar -zxf "waf-${WAF_VERSION}.tar.gz" &&
          cd "waf-${WAF_VERSION}" &&
          ./waf-light configure build &&
          cd ${HOME} &&
          mv "waf-${WAF_VERSION}" waf &&
          cd $GITHUB_WORKSPACE; }

      - name: Build and Test
        run: cd cpp && ${HOME}/waf/waf configure test
