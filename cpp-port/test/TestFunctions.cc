#include "common.h"
#include <gtest/gtest.h>

TEST( TestFileSequence, FramesToFrameRange ) {
    TODO();
}

TEST( TestFileSequence, PaddingChars ) {
    TODO();
}

TEST( TestFileSequence, PaddingCharsSize ) {
    TODO();
}

TEST( TestFileSequence, ToRange ) {
    TODO();
}

TEST( TestFileSequence, FindSequencesOnDisk ) {
    TODO();
}

TEST( TestFileSequence, FindSequenceOnDisk ) {
    TODO();
}

TEST( TestFileSequence, HandleSymlinksOnDisk ) {
    TODO();
}
