#include "common.h"
#include <gtest/gtest.h>

TEST( TestFrameSet, New ) {
    TODO();
};

TEST( TestFrameSet, Lookup ) {
    TODO();
};

TEST( TestFrameSet, Padding ) {
    TODO();
};

TEST( TestFrameSet, Normalize ) {
    TODO();
};

TEST( TestFrameSet, Inverted ) {
    TODO();
};

