#include "common.h"
#include <gtest/gtest.h>

TEST( TestFileSequence, New ) {
    TODO();
}

TEST( TestFileSequence, Split ) {
    TODO();
}

TEST( TestFileSequence, FrameIndex ) {
    TODO();
}

TEST( TestFileSequence, SetDir ) {
    TODO();
}

TEST( TestFileSequence, Format ) {
    TODO();
}

